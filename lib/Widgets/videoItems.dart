import 'package:flutter/material.dart';

Widget videoItem() {
  return Container(
    height: 200,
    color: Colors.grey,
    child: Center(
      child: Icon(
        Icons.play_arrow,
        color: Colors.pink,
      ),
    ),
  );
}
